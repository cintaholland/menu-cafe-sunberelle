// script.js - Anda bisa menambahkan fitur interaktif di sini nanti,
// seperti filter menu, animasi, atau shopping cart (jika ini aplikasi web yang lebih kompleks).

// Contoh: Log sederhana saat halaman dimuat
document.addEventListener('DOMContentLoaded', () => {
    console.log('Halaman menu berhasil dimuat!');
});